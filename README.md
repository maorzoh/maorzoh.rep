# Horizon CS Monitor Tool

Horizon Connection Server Monitor provides a centralized, visual dashboard for tracking the availability of multiple Connection Servers.
By performing automated HEAD requests to the /favicon.ico endpoint, the tool verifies that the web service is responsive and that the Horizon Admin Console is ready to use.
